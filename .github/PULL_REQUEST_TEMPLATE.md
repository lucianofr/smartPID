## Summary

<!-- One paragraph: what this PR changes and why. -->

## Type of change

<!-- Delete what does not apply. -->

- [ ] Bug fix
- [ ] New feature
- [ ] Refactor (no behavior change)
- [ ] Docs / CI / tooling

## Checklist

- [ ] Backend: `uv run ruff check .` and `uv run pytest -q` pass
- [ ] Web: `npm test` (vitest) passes and `npm run build` succeeds
- [ ] `gen:api:check` clean when API types changed (`npm run gen:api:check`)
- [ ] No secrets or local paths committed (`.env*`, `*.local`)
- [ ] Screenshots/evidence attached for UI changes (see `test-evidence/`)

## Test evidence

<!-- Concrete commands run + observed results, or links to test evidence. -->

## Related

<!-- Issues, PRs, plans: `#123`, `plans/00X-*.md`, docs links. -->
