# Report Registry

Index of completed agent work. One line per report. See `_tech-debt.md` for
what was deliberately deferred.

## bugs

- bugs-simulator-registration-20260730 | Done | A controller created through `POST /controllers` was never registered with the simulator, so every `/simulator/*` route failed until a daemon restart. Registration and a new `unregister_controller` now hook the create/delete routes; three routes that leaked a bare `KeyError` as a 500 and one that returned 200 for an unknown id share a single `_sim_controller` guard. RED proof against pristine HEAD: 16 failed / 2 passed before, all green after.
- bugs-simulator-registration-20260730 (follow-up) | Done | CO read-through, quality propagation and mode reporting. `pid_worker._drain_telemetry` seeded CO from the first frame only, and MAN is the only branch that never reassigns it, so `co` published 0.0 GOOD forever — and froze `integral_val` with it. Mirrored the SP ownership block (whose comment already named a CO mirror that did not exist), switched unmapped-tag substitution from `FFSignal.good(0.0)` to `bad`, published the monitored controller's mode in SUPERVISORY, and gave simulator-registered controllers their own `mode_int_map`. One existing test needed editing, as predicted.
- bugs-sim-config-schema-lock-20260730 | Done | `Configuracao_Simulador` shipped in three generations and the `pid_*` group never got a back-fill, so gen-1 project files failed the 17-column INSERT. Migration now covers every column with `PRAGMA table_info` idempotency. The `database is locked` failures were proved (300 samples, 20.2 h, exact 5.0 s minimum inter-failure gap) to be full-budget waits rather than snapshot-bypass, so `busy_timeout` 5 s to 15 s is a real fix. A failed persist no longer surfaces as a 500 when the in-memory change succeeded.
- bugs-sim-config-schema-lock-20260730 (follow-up) | Done | The root enablers. `_retention_cleanup` slept 24 h *before* its first pass, so a daemon restarted more often than daily never pruned `Log_Processo` — the reason the active project file reached 1.08 GB. Inverted to pass-first, with deletes chunked at a measured 5,000 rows/batch (72 ms lock hold vs 361 ms at 50,000). Also fixed an `isoformat()` vs `datetime('now',...)` comparison that left rows just past the cutoff permanently unprunable, and stopped a lock error in `DBWorker._flush()` from silently killing the telemetry thread.
- bugs-co-telemetry-mapping-20260730 | Done (diagnosis only) | Adjudicated whether `co == 0` in SUPERVISORY was defect or design. Verdict DEFECT, with wire-level evidence: the twin's `CTRL_2/PID/CO` node read 100.0 Good at the instant `STATUS.2.co.value` was 0.0. Corrected two framing assumptions — PIDWorker is the publisher, not MonitorWorker, and the twin's node is fine — which is what located the actual line. Patch routed to the Backend specialist.
- bugs-simulator-pid-settings-inject-20260730 | Done | Two follow-up complaints on the same-day PID-settings/diagram work. (1) `PIDSettings.tsx` disabled Kp/Ti/Td and Apply whenever `pid_enabled` was false — but the backend accepts and stores those params regardless of enable state (only the tick loop's use of them is gated), so this blocked the normal workflow of staging tuning values before arming the internal PID. Removed the gating; fields are always editable, Apply always live. (2) "Inject disturbance" reported as silently not working — root-caused live against the real backend: `Auto-disturbance` (`AutoToggles`) was enabled on the test loop and was re-randomizing `step_amplitude` on its own cycle (confirmed: a manual `amplitude:10` injection read back as `16.886...` seconds later, then a clean re-test with auto off round-tripped the exact typed amplitude). Manual injection was never broken — it was racing an automation feature with zero indication. Closed the path instead of leaving it to silently lose the race, mirroring the codebase's existing "closed rather than ignored" precedent for CO-in-AUTO: type/amplitude/Inject now disable with a visible hint whenever `auto_disturbance.enabled` is true. 34 vitest tests green (+2 new: PID fields stay enabled while off, injection closes under auto-disturbance), `tsc -b` clean, both fixes re-verified live (auto-disturbance on → Inject visibly closed with hint; auto-disturbance off → exact typed amplitude persists).
- bugs-simulator-pid-auto-not-driving-20260730 | Done | "PID em AUTO não está alterando a saída CO mesmo com erro" — reproduced live against the real running daemon, not assumed. Root cause 1 (primary): two independent switches gate the internal PID (`pid_enabled` admin-only via `/pid/enable`, `pid_mode` via `/pid/mode`, operator-reachable) and the tick loop requires BOTH; selecting AUTO alone (`pid_enabled=false`) or enabling alone (`pid_mode=MAN`) each leave CO permanently frozen with zero UI feedback — proved both dead-end combinations directly against `/simulator/status` (error=80, CO stuck at 0 in either case). Fixed by coupling `onSetMode` in `SimulatorControlPanel.tsx`: selecting AUTO now also arms the internal PID for a session that already reaches the config region (`canConfigure` guard — `/pid/enable` is admin-only, so an operator's AUTO click is left unchanged, no new 403). Root cause 2 (adjacent, real): `SimulatorAdapter.enable_pid()`'s `bumpless_transfer(..., current_pv=0.0, ...)` hardcoded the PV baseline instead of the real `ctrl.live_pv`, defeating the whole point of a *bumpless* transfer (manufactures a phantom error_prev/pv_prev on every arm). Fixed to pass `ctrl.live_pv`. Verified with a clean controlled test first (Kp=1/Ti=10/Td=0, SP=80 vs PV≈0 via direct API calls): CO climbed 0 → 84.8 → 88.9 → 92.8 → 96.8 → 100 over 2.5 s once both flags were correctly set, proving the PID math itself was never broken. Backend suite 127/128 green (the 1 failure is the pre-documented `test_opcua_start_stop` port-4849 conflict with the live dev daemon, not a regression). Frontend 15/15, `tsc -b` clean. Re-verified end to end after a full daemon restart: one real UI click on AUTO now fires both `/pid/mode` and `/pid/enable`, and CO climbed 0 → 92 in 1.5 s under load.
- bugs-frontend-trend-clip-tabs-20260730 | Done | The trend card's x-axis ruler was clipped because `MIN_PLOT_HEIGHT` floored the *canvas* while the well's height is content-driven, so a 168 px well rendered inside a 114 px box and 54 px escaped the card. Ruled out the two likelier candidates by measurement (the annotation had 4 px of clearance; the border rendered and was merely occluded). Fixed with `max-h-full` plus a floor of 72, and split the new E2E guard into two independent assertions after discovering `max-h-full` alone silently clips the canvas instead. Also removed a dead `border-accent` declaration kept alive only by an out-of-date test.

## implementation

- implementation-simulator-loop-diagram-pid-settings-20260730 | Done | The `/simulator` page was missing a diagram of the loop being simulated and any UI for the twin's internal test PID (backend already fully implemented per SIM-6/`docs/superpowers/specs/2026-04-06-simulator-internal-pid-design.md` — 4 REST routes and `ControllerSimStatus.pid_*` fields existed, but the React web client only ever wired mode/SP/CO, never enable/Kp/Ti/Td). Two parallel builders added `ClosedLoopDiagram.tsx` (hand-rolled SVG, SP→Σ→PID→CO→Processo→PV with a dashed feedback path and a disturbance-injection arrow, live-annotated from the twin snapshot when readable, degrading to the generic topology for a restricted operator) and `PIDSettings.tsx` (Enable toggle + Kp/Ti/Td draft+Apply, wired to the pre-existing `/pid/enable` and `/pid/params` routes). Two independent specialist reviews (react-reviewer, typescript-reviewer) each caught real, non-overlapping issues, all fixed: a missing `key={controllerId}` that let a stale Kp/Ti/Td draft from one loop silently apply to a different loop after a selector switch (RED→GREEN proved with a new regression test before shipping), an `aria-hidden` that dropped the Ti/Td unit from screen readers (fixed by moving the unit sibling outside the `<label>` instead of hiding it, preserving the bare accessible name existing tests depend on), and both reviewers independently flagging that the two new PID request types landed in `types.ts` but were never consumed by `api.ts` (now typed against the generated OpenAPI schema via `satisfies`/`Omit<...>` instead of hand-declared shapes). Verified live against the real backend (not just mocks): production build, logged in as admin, created a controller, started the simulator, confirmed the diagram's live annotations and purple disturbance highlight against a direct `GET /simulator/status` call, then round-tripped a real Kp edit through Apply and an Enable-PID toggle through the actual API. 33 targeted vitest tests (was 32, +1 regression), 4 Playwright e2e specs, `tsc -b` all green; one real visual bug (two overlapping SVG "PV" labels colliding with the feedback line) found and fixed during the live check. `PRD.md` §5.4 updated (SIM-6 amended to name the UI surface, SIM-8 added for the diagram). All uncommitted on `main`.
- implementation-simulator-trend-legend-diagram-polish-20260730 | Done | Three follow-up polish requests on the simulator page. (1) Trend chart had no color key: added a swatch (matching `TrendPanel.tsx`'s existing `LegendItem` pattern exactly — same `trace-pv`/`trace-sp`/`trace-co` tokens, solid for PV/CO, dashed for SP) beside each PV/SP/CO readout in `TwinTrend.tsx`'s header, so the legend is guaranteed to match the real chart lines by construction, not a hand-picked color. (2) Y axes were unlabeled (`Trend.tsx` only ever rendered the unit, "%", on both axes — indistinguishable). Added an optional `name` field to the shared `TrendAxisConfig` + an `axisLabel()` helper combining name+unit ("PV / SP (%)", "CO (%)"); wired only in `TwinTrend.tsx` (dashboard `TrendPanel.tsx` untouched, out of this request's scope, still backward compatible). (3) The diagram's Processo box (110px) couldn't fit its parameter text, especially with τ2 present or raw unrounded backend floats (observed: `pid_kp` values with 15+ decimal digits from auto-excitation). Added a `fmt()` 2-decimal helper (matching `DynamicsSliders`' own `.toFixed(2)` convention) applied to every displayed number in the diagram, widened Processo from 110→140px, split its parameter line into two, and shifted every downstream coordinate (Σ2 junction, disturbance arrow, PV arrow/label, feedback path) +30 units to clear the wider box — verified no extrapolation beyond any rect boundary. One self-caused casualty during a 5-hunk single edit: a boundary mismatch duplicated/broke the Perturbação label block; caught immediately via `tsc -b` and a fresh full-file read, repaired against the actual broken state rather than guessed. 19 test files / 78 tests green, `tsc -b` clean, all three confirmed live in a rebuilt browser session with real computed-style color checks on the swatches.

## tests

- tests-e2e-testsprite-20260730 | Done | TestSprite MCP against smart_pid_web: first run 15/15 BLOCKED (app unreachable — vite bound `127.0.0.1` only, this sandbox resolves `localhost` to IPv6 first). Fixed dual-stack bind, an `allowed_ws_origins` gap that silently auto-logged-out every `localhost`-origin session, and a `tsc -b` misconfiguration that let a stale compiled `vite.config.js` silently shadow the real `.ts` source for every later dev/preview run. Switched to production preview after dev-mode crashed under concurrent test load. Four full 30-test runs after fixes scored 18/22/21/21 PASSED with a different subset failing each time; every one of the 30 cases passed in at least one run except TC027 (real `.spid` file unavailable to the sandbox) and TC011 (tests a per-alarm footer control that does not exist by design — `AlarmFooterBar.tsx` only has ACK ALL). Every failure across all four runs independently reproduced as correct app behavior (shared-resource test races on the single global OPC-UA connection/active project/alarm queue, two wait-timing false positives) — zero application defects found after the infra fixes. See report for full evidence.

## review

- review-L1-peer-20260731 | Done | L1 peer review of the uncommitted 13-file change set (+512/−197). No BLOCKING. Verified rather than assumed: ran 69 tests, traced that `repo.reopen()` mutates in place (so `_resync_simulator_link` queries the NEW project), that `OPCUAAdapter.start()` is idempotent, that `bind_opcua_client`'s empty-node-id path is guarded, and that boot-time node population is synchronous. Refuted two coordinator preliminaries: the bare `except` in `controllers.py` is pre-existing, and the `hasattr` guards match the class's established style. Highest-value find (NB1) was a functional bug the coordinator had missed: `SimulatorAdapter.stop()` never clears `_controllers` and `register_controller` is insert-if-absent, so twin state leaks across project switches. Test coverage per fix: 2/4/5 covered, fix 3 has none, fix 1 covered at 1 of 3 call sites.
- review-L2-arch-20260731 | Done | L2 architecture review. 1 BLOCKING, 6 non-blocking, 2 nits; merge blocked. Ruled L1's NB1 a missing *lifecycle contract* — the twin has per-entity add/remove but no project-boundary operation, and `ProjectService` has no reconciliation step, so `_controllers` is the union of every project opened since boot with first-writer-wins values. Split the remedy deliberately: capability (`reset_to(ids)`) belongs on the adapter, the decision to invoke belongs to `ProjectService`; explicitly NOT on `stop()`, which is the operator's pause button and would trade a state-leak bug for state loss. Caught a trap in its own fix: the upsert must guard `opcua_server.register_controller` on `controller_node_ids`, not `_controllers`, or `add_folder` mints a duplicate OPC-UA folder. Downgraded the layering concern — `db_worker.py` already imports `adapters.outbound.*`, so only the *inbound* direction is new, and it is free to remove by making `bind_opcua_client` a method. Rejected widening the domain `TelemetrySource` port with OPC-UA addressing.
- review-full-review-20260731 | Done | Aggregated summary across all four levels. Verdict CHANGES REQUESTED on three deduplicated blocking issues: B1 twin state leak (converged from L1+L2+L4), B2 `FiniteFloat` absent from `ControllerCreate`/`ControllerUpdate`, B3 `zmq.ZMQError: break` never checks errno. Coordinator independently confirmed B1's two mechanics and B2's zero-occurrence grep. Working tree verified byte-identical to the frozen diff afterwards — the review made no source edits.

## security

- security-L3-security-20260731 | Done | L3 security review. 1 BLOCKING: `FiniteFloat` is applied nowhere on `ControllerCreate`/`ControllerUpdate`, so fix 4 hardened only the `commands.py` DTOs and left the sibling admin routes exposed — reproduced live: NaN on `scan_rate_s` gives an unhandled 500 `sqlite3.IntegrityError`, Infinity on `pid_params.gain` gives 201 Created with `inf` genuinely persisted while every read reports `null` (Pydantic v2 `ser_json_inf_nan`), so the operator sees neither an error nor the real value. Settled the recursion question by isolating the two calls: the `RecursionError` originates in `jsonable_encoder` (pre-existing FastAPI, called identically by the stock handler) at depth ≥995, before `_json_renderable` ever runs — real but not introduced here, and bounded to one authenticated request. Auth verdict: the 422 handler is an AUTHENTICATED surface — `solve_dependencies()` resolves `Depends()` before `request_body_to_args()`, probe-confirmed (401, never 422). Also refuted the premise that FastAPI's default handler echoes `exc.body` — zero grep matches in the installed 0.135.3.

## sre

- sre-L4-reliability-20260731 | Done | L4 reliability review. 2 BLOCKING. Central verdict: the new catch-alls RELOCATE rather than close the "dead worker looks like a steady loop" gap — a worker that survives but retries forever is equally indistinguishable, because nothing (`/system/status`, `LoopManager.is_loop_running`, `get_stats_workers`, `AlarmWorker`) reads liveness or a failure count. Blocking: `except zmq.ZMQError: break` never inspects `errno`, so a transient EINTR (a profiler attaching to diagnose a stuck worker) kills the thread with zero log output and no supervisor; and L1's NB1 rated most dangerous item in the set, producing plausible wrong data with no error path, with compounding risk if tuning derived against the wrong twin dynamics reaches a real DCS. Verified the "mirrors `PIDWorker._loop`" comment is TRUE — which is the problem, as it imports that loop's unbounded-retry gap into two more threads. Corrected the brief's premise on partial failure: the project router's narrow `except` yields 500/400, not a false 200. Confirmed by direct read that `finally: sub.close(); pub.close()` runs on every exit path.

## Conventions confirmed this round

- Every subagent ran on `model: "opus"` per `CLAUDE.md`.
- No agent committed or switched branches; all work landed uncommitted on
  `fix/simulator-registration-and-schema` for review.
- `test_opcua_start_stop` fails environmentally whenever a live daemon holds
  port 4849. Stop the daemon before a full-suite run.

---

## 2026-08-05

- sre/sre-vps-deploy-20260805 | Completed | smartPID deployed on VPS 76.13.172.133 as container `smartpid` at `0.0.0.0:8032->8000/tcp`, non-root uid 1000, named volume `smartpid-data:/data`, env via `/opt/smartpid/.env` (mode 0600, fresh 64-hex JWT secret). Multi-stage Dockerfile (node:20 web build with `VITE_API_BASE=""`, python:3.13 + uv frozen). Verified end-to-end from the workstation: SPA 200, assets 200, bundle has no `/api/` prefix, login 200 with the generated bootstrap password, old admin/admin 401, /docs·/openapi.json 404, restart durability 200.
- security/security-vps-exposure-20260805 | Completed | Read-only audit of the public-8032 exposure. All four unauthenticated reachability questions answer NO (only GET /system/status public, no process values); every one flips to YES with seeded creds. TD-010 verified fixed, TD-011 verified open at audit time.
- security/security-bootstrap-admin-20260805 | Completed | `_seed_default_admin` no longer hard-codes `admin`: explicit `SPID_BOOTSTRAP_ADMIN_PASSWORD` (never logged) or `secrets.token_urlsafe(12)` logged once as `bootstrap_admin_password`. Resolves TD-011. 7/7 green.
- security/security-login-hardening-20260805 | Completed | `LoginRateLimiter` (5/60 s sliding window, per-IP by design) on `/auth/login`; `LoginRequest` capped username ≤254 / password ≤200. Bcrypt compare already constant-time. 50/50 green.
- security/security-openapi-shadow-20260805 | Completed | /docs, /redoc, /openapi.json 404 by default behind `SPID_API_EXPOSE_OPENAPI` (default false). `dump_openapi.py` unaffected. 2/2 green.
- docs/docs-readme-health-endpoint-20260805 | Completed | Windows installer README verification corrected from nonexistent `/health` to `GET /system/status`.

## 2026-06-18

<!-- Format: - report-name | Status | One-line summary -->
<!-- Example: - review-auth-timeout-20251228 | Completed | Root cause and fix plan -->

- arch/arch-web-hmi-20260618 | Completed | RealtimeWS bridge: CRITICAL naive `await recv` blocks daemon; STATUS.{id} is the live frame (not TELEMETRY); discrete events must not coalesce; StaticFiles/CORS + OPC-via-REST; zmq.asyncio + ConnectionManager.
- review/review-web-hmi-contract-20260618 | Completed | Contract-accuracy audit of fatia specs vs backend — wrong routes/topics, missing endpoints, GAPs (enable PID, OPC start/stop, export list).
- security/security-web-hmi-20260618 | Completed | CRITICAL: project router unauth + path traversal; HIGH: /commands/tuning guardrail bypass, no CORS/TrustedHost (0.0.0.0), no upload size limit; MEDIUM: WS `?token=`.
- tests/tests-web-hmi-20260618 | Completed | Test-plan & acceptance-criteria review — replace non-verifiable prose (≈60 fps, "paridade visual") with objective asserts; RBAC negative tests; ack≠clear.
- design/design-web-hmi-system-20260618 | Completed | Design review of the web HMI design system (tokens/components/themes), authority for all fatias.
- spec: docs/superpowers/specs/2026-06-18-web-frontend-design-system-design.md | Created | Web frontend design-system spec — single source of UI tokens, components and themes for the 8 fatias.

---

---

## Archive

Older entries are moved to: `reports/archive/_registry-archive.md`
